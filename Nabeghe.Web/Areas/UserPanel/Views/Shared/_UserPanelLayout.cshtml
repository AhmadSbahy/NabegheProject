@using Nabeghe.Application.Extensions
<!DOCTYPE html>
<html lang="fa" dir="rtl">
<head>
	<partial name="_MetaTags" />
	<link rel="icon" type="image/svg+xml" href="/assets/images/favicon.svg" />
	<partial name="_StyleReferences" />
	<title>@ViewBag.Title | سایت اموزشی نابغه</title>
</head>
<body>
	<div class="flex flex-col min-h-screen bg-background">
		<partial name="_PageHeader" />
		<main class="flex-auto py-5">
			<div class="max-w-7xl space-y-14 px-4 mx-auto">
				<div class="grid md:grid-cols-12 grid-cols-1 items-start gap-5">
					@await Component.InvokeAsync("UserPanelSideBar", new { userId = User.GetUserId() })
					@RenderBody()
				</div>
			</div>
		</main>
		<partial name="_Footer" />
	</div>
		<partial name="_JsReferences" />
		<partial name="_SiteNotification" />
	@RenderSection("Scripts", false)
</body>
</html>